<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Thème foncé de l'application -->
    <style name="AppThemeDark" parent="Theme.MaterialComponents.NoActionBar">

        <item name="colorPrimary">@color/colorPrimaryDarkMode</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark_DarkMode</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Thème des paramètres -->
    <style name="SettingsTheme" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Thème foncé pour les paramètres -->
    <style name="SettingsThemeDark" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="android:textColor">#fff</item>
        <item name="android:textColorSecondary">@color/colorPrimaryDarkMode</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Thème foncé pour l'écran de choix du bluetooth -->
    <style name="bluetoothScreenThemeDark" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="android:textColor">#fff</item>
    </style>

    <!-- Thème foncé des alertDialog-->
    <style name="alertDialogDark" parent="Theme.AppCompat.Dialog.Alert">

        <item name="android:background">@color/colorPrimaryDarkMode</item>
    </style>

    <!-- Thème du mode plein écran-->
    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>
</resources>